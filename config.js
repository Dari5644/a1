// config.js
// كل الإعدادات هنا (بدون مفاتيح حساسة)

export const config = {
  // معلومات المتجر
  STORE_NAME: 'smart bot',
  STORE_URL: 'https://vamocu.zid.store/',

  // رابط لوحة التفعيل (الموقع اللي تبي العميل يفتح منه رابط التفعيل)
  // غيّر هذا إلى دومين موقعك مثلاً: https://bots.aldeem35.com
  PORTAL_BASE_URL: 'https://a1-9b9e.onrender.com',

  // إعدادات زد
  ZID_API_BASE: 'https://api.zid.sa/v1',
  ZID_POLL_INTERVAL_MS: 60_000, // كل دقيقة يفحص الطلبات (تقدر تغيّر)

  // ملفين لتخزين البيانات بشكل بسيط
  PROCESSED_ORDERS_FILE: './zid-orders.json',      // الطلبات التي تم التعامل معها
  ACTIVATIONS_FILE: './activations.json',          // روابط التفعيل (التوكنات)

  // إعدادات واتساب (القيم نفسها في .env)
  WHATSAPP_PHONE_ID: process.env.PHONE_ID,
  WHATSAPP_TOKEN: process.env.WABA_TOKEN,

  // تعريف المنتجات (من زد) والمدة ونوع البوت
  PRODUCTS: {
    // 🟢 بوت واتساب لمدة شهر
    WHATSAPP_1M: {
      zidProductId: 'Z.17632374787413204', // عدّلها لـ ID المنتج الحقيقي في زد
      type: 'whatsapp',
      months: 1,
      label: 'بوت واتساب – اشتراك شهر واحد',
    },

    // 🟢 بوت واتساب لمدة 3 شهور
    WHATSAPP_3M: {
      zidProductId: 'Z.XXXXXXXXXXXXXXX', // حط ID منتج 3 شهور
      type: 'whatsapp',
      months: 3,
      label: 'بوت واتساب – اشتراك 3 شهور',
    },

    // 🔵 بوت تليجرام
    TELEGRAM_1M: {
      zidProductId: 'Z.TELEGRAM_PRODUCT_ID', // عدّلها
      type: 'telegram',
      months: 1,
      label: 'بوت تليجرام – اشتراك شهر واحد',
    },

    // 🟣 بوت ذكاء اصطناعي للموقع / المتجر
    STORE_AI_1M: {
      zidProductId: 'Z.STORE_AI_PRODUCT_ID', // عدّلها
      type: 'store_ai',
      months: 1,
      label: 'بوت ذكاء اصطناعي للمتجر – شهر واحد',
    },
  },
};
